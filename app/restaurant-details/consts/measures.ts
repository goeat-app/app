import { Dimensions } from 'react-native';

export const { height: SCREEN_HEIGHT } = Dimensions.get('window');

export const IMAGE_HEIGHT = SCREEN_HEIGHT * 0.45;
export const BACK_BUTTON_PADDING = 124;
export const SHEET_OVERLAP = 80;
